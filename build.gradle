plugins {
    id 'groovy'
    id 'maven'
    id "com.gradle.plugin-publish" version "0.9.7"
}

group = 'com.polidea.android'
version = '1.0.0'

sourceCompatibility = 1.6

repositories {
    jcenter()
}

configurations {
    provided
}

sourceSets {
    main {
        compileClasspath += configurations.provided
    }
}

dependencies {
    compile gradleApi()
    compile localGroovy()
    provided 'com.android.tools.build:gradle:2.2.3'
    compile 'org.yaml:snakeyaml:1.14'
}

pluginBundle {
  website = 'https://github.com/Polidea/gradle-config'
  vcsUrl = 'https://github.com/Polidea/gradle-config'
  description = 'Gradle plugin to add multi-variants yaml settings to Android.'
  tags = ['gradle', 'plugin', 'android', 'config']

  plugins {
    configPlugin {
      id = 'com.polidea.android.config'
      displayName = 'Gradle Config plugin for android applications and libraries.'
    }
  }

    mavenCoordinates {
        groupId = "com.polidea.android"
        artifactId = "config"
    }
}

install {
    repositories.mavenInstaller {
        pom.version = project.version
        pom.artifactId = 'config'
    }
}
